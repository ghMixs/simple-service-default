#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Build '){
            steps {
                script {
           
                        echo "Build succes"
                        echo "${env.BRANCH_NAME}"
                        echo "${env.GIT_COMMIT}"
                         echo "${GIT_COMMIT}"
                      
                    }
                }         
        }  
    }
}
