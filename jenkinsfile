#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Build '){
            steps {
                script {
           
                        echo "Build succes"
                        echo ${BRANCH_NAME}
                      
                    }
                }         
        }  
    }
}
